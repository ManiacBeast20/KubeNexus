apiVersion: v1
kind: ConfigMap
metadata:
  name: kubenexus-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
    release: monitoring
data:
  kubenexus-dashboard.json: |-
{{ .Files.Get "dashboards/kubenexus-dashboard.json" | indent 4 }}
